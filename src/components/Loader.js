import React from 'react';
import { Loader } from 'semantic-ui-react';

const Loading = () => <Loader className="loader" active inline='centered' size='big'>Cargando...</Loader>

export default Loading;
